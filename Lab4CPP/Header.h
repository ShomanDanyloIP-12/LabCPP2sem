#pragma once

#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <ctime>

using namespace std;

bool isNumber(string str);
bool isInterval(string str);
vector <int> IntervaltoForm(string num);